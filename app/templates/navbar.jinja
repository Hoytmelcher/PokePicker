<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ url_for('main.index') }}">PokePicker</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="{{ url_for('main.index') }}">Home</a>
        {% if current_user.is_anonymous %}
        <a class="nav-link" href="{{ url_for('auth.register') }}">Sign Up</a>
        <a class="nav-link" href="{{ url_for('auth.login') }}">Sign In</a>
        {% else %}
        <a class="nav-link" href="{{ url_for('poke.pick_pokemon') }}">Pokemon</a>
        <a class="nav-link" href="{{ url_for('user.user', username=current_user.username)}}">Profile</a>
        <a class="nav-link" href="{{ url_for('poke.user', username=current_user.username)}}">Collection</a>
        <a class="nav-link" href="{{ url_for('user.post') }}">Post</a>
        <a class="nav-link" href="{{ url_for('auth.log_out') }}">Logout</a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>